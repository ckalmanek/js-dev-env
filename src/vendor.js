/* This file contains references to vendor libraries used in our app. 
   This is used by webpack in the production build only, to allow users
   to only download the vendor code when a dependency has changed 
*/

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
